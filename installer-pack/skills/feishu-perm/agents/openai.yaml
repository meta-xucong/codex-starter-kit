interface:
  display_name: "文件权限"
  short_description: "检查飞书文件共享状态，并安全变更协作者权限与所有权设置。"
  default_prompt: "Use $feishu-perm to inspect the collaborators and apply this precise permission change."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "feishu"
      description: "Official Feishu/Lark OpenAPI MCP server"
      transport: "stdio"
