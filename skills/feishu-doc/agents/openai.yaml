interface:
  display_name: "云文档"
  short_description: "通过官方飞书 MCP 只读获取云文档，并分析正文与块结构内容。"
  default_prompt: "Use $feishu-doc to read and summarize this Feishu document."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "feishu"
      description: "Official Feishu/Lark OpenAPI MCP server"
      transport: "stdio"
