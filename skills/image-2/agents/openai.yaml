interface:
  display_name: "图像生成连接"
  short_description: "调用已配置的图像服务，安全生成、编辑并核对图片产物与费用。"
  default_prompt: "Use $image-2 to prepare and confirm this image generation request before submitting it."
policy:
  allow_implicit_invocation: false
